import { StyleSheet } from "react-native";

export const AppStyles = StyleSheet.create({
    App: {
        padding: 16,
        paddingBottom: 0,

        width: 100,
        minHeight: 100,

        flexDirection: "column",
        gap: 16,
    },
});
